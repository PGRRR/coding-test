package inflearn;

import java.util.Arrays;

public class CodingTestAdventureGuild {
    public static void main(String[] args) {
        int n = 5;
        int[] arr = {2, 3, 1, 2, 2};
        Arrays.sort(arr);
        int count = 0;
        for (int i = 0; i < arr.length; i++) {
            if (arr[i] == i) {
                
            }
        }
    }
}
